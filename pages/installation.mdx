---
title: Installation
---

# Installation

Hello and hi

## Next.JS Template

The easiest way to get started with `use-cardano` is the starter template. It's a Next.JS project that has `use-cardano` installed and configured. You can use it as a starting point for your own project.

## Integrating into an existing project

If you already have a project and want to integrate `use-cardano` into it, you can do so by following the steps below.

## Peer dependencies

`use-cardano` has a few peer dependencies that you'll need to install in your project. These are:

### React

Since 

### Lucid

Lucid is the engine that powers `use-cardano` as far as Cardano blockchain interactions go. Install I highly recommend 